# Values are EXAMPLES; never commit .env or real credentials.
# The application reads OS environment variables; .env is NOT loaded automatically.
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=
ALPHAVANTAGE_API_KEY=
OPENAI_API_KEY=
# Choose a model available to your OpenAI project; no model is assumed.
OPENAI_MODEL=
# Generate a random token of at least 32 characters for the local HTTP API.
FOREX_API_TOKEN=
